class ProfilesController < ApplicationController
  def show
    # If you want to load data (e.g., @user = User.find(...)), do it here.
    # Otherwise you can leave this empty and just render the view.
  end
end